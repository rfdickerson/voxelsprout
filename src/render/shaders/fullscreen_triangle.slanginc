#ifndef VOXEL_FULLSCREEN_TRIANGLE_SLANGINC
#define VOXEL_FULLSCREEN_TRIANGLE_SLANGINC

float2 fullscreenTriangleNdc(uint vertexId)
{
    if (vertexId == 0u) return float2(-1.0, -1.0);
    if (vertexId == 1u) return float2(3.0, -1.0);
    return float2(-1.0, 3.0);
}

#endif
